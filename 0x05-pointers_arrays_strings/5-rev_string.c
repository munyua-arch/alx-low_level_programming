#include "main.h"

/**
 * rev_string - function to reverse a string
 * @s: variable
 * Return: void
 */
void rev_string(char *s)
{
	int count = 0;
	int k;
	char rev = s[0];

	while (s[cpounter != '\0'])
		counter++;
	for (k = o; k < count; k++)
	{
		count--;
		rev = s[i];
		s[i] = s[counter];
		s[counter] = rev;
	}
}
