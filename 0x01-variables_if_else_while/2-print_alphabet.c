#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;
	char z;

	while (a < z)
	{
		putchar("a\n");
		a++;
	}
	return (0);
}
